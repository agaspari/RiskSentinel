package com.risksentinel.analyst;

/**
 * Placeholder entry point for the analyst module. Task 8.2 will wire a real
 * LangChain4j-backed {@link AnalystAgent} here; for now this exists so the
 * application plugin in {@code build.gradle.kts} has a {@code mainClass} that
 * compiles and runs.
 */
public final class Main {

    private Main() {}

    public static void main(String[] args) {
        System.out.println("RiskSentinel analyst — agent not yet wired (Phase 8 task 8.2).");
    }
}
